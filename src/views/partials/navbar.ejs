
<nav class="navbar navbar-expand-lg navbar-light p-3 shadow-sm mt-0 mb-5 bg-white container-fluid w-100">    
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> 
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
      
        
        <% if(typeof user == 'undefined') { %>
          <div class="">      
            <a class="btn btn-outline-primary" href="#" onclick="location.replace('/login')">Iniciar Sesion</a> 
            <a class="btn btn-outline-primary" href="#" onclick="location.replace('/signup')">Crear Cuenta</a>         
          </div>
          <% } %>
          <% if(typeof user != 'undefined') { %>
          <li class="nav-item dropdown">
            <a class="nav-link" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="bi bi-gear"  style="font-size: x-large;"></i>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">          
              <a class="dropdown-item" href="#" onclick="location.replace('/perfil')">Perfil</a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="#" onclick="location.replace('/logout')">Cerrar Sesión</a>
            </div>
          <% } %>

        </li>      
      </ul>   
 
</div>
  </nav>